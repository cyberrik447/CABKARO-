package com.example.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// Light Soft Premium Theme Colors
val PremiumSlate = Color(0xFF1E232C)       // Primary branding Slate
val WarmGold = Color(0xFFC5A880)           // Elegant Gold Accent (Not neon)
val PlatinumGray = Color(0xFFF3F4F6)       // Soft background/surface
val CharcoalDark = Color(0xFF12141C)       // Deep charcoal for high contrast
val IvoryWhite = Color(0xFFFAFAFA)         // Soft Warm White

// Dark and Pure-Black Colors (for future support)
val PureBlack = Color(0xFF000000)
val PureWhite = Color(0xFFFFFFFF)
val MutedSlate = Color(0xFF64748B)         // Secondary gray
val ErrorRed = Color(0xFFEF4444)
